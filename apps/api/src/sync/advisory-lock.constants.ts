export enum AdvisoryLockNamespace {
  SYNC_RUN = 1,
  TDX_QUOTA = 2,
}

export enum SyncResourceLockId {
  ROUTES = 1,
  STOPS = 2,
  STATIONS = 3,
  SHAPES = 4,
}

export enum TdxQuotaLockId {
  GLOBAL = 1,
}
