{
  "name": "bus",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare": "husky",
    "build": "pnpm -r --if-present build",
    "build:packages": "pnpm --filter './packages/*' -r --if-present build",
    "dev": "concurrently -k -n proxy,web \"pnpm --filter @bus/tdx-proxy dev\" \"pnpm --filter @bus/web dev\"",
    "dev:api": "concurrently -k -n proxy,api,web \"pnpm --filter @bus/tdx-proxy dev\" \"pnpm --filter @bus/api start:dev:local-api\" \"pnpm --filter @bus/web dev:local-api\"",
    "dev:mobile": "concurrently -k -n proxy,web \"pnpm --filter @bus/tdx-proxy dev:mobile\" \"pnpm --filter @bus/web dev:mobile\"",
    "format": "pnpm -r --if-present format",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "husky": "^9.1.7",
    "prettier": "^3.4.2"
  }
}
